import Helmet from 'react-helmet';
import React, {Fragment} from 'react';

const Home = () => (
  <Fragment>
    <Helmet>
      <title>Informing voters</title>
    </Helmet>
    <div>Hello hey</div>
  </Fragment>
);

export default Home;
