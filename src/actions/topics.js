import {createAction} from 'redux-actions';

export const reorder = createAction('TOPICS_REORDER');
export const success = createAction('TOPICS_SUCCESS');
export const failure = createAction('TOPICS_FAILURE');
